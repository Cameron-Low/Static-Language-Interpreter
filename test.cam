// Fibonacci up to the 20th item.
let max be num;
let count be num;
let fNum be num;
let sNum be num;
let temp be num;

max = 20;
count = 0;
fNum = 1;
sNum = 1;

while count != max do
    show fNum;
    temp = fNum + sNum;
    fNum = sNum;
    sNum = temp;
    count = count + 1;
endwhile